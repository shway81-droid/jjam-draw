# 짬짬이 그리기 MVP 제품 요구사항 정의서(PRD)

- **버전:** 1.0
- **형태:** 정적 웹 MVP
- **저장소:** `jjam-draw` (신규)
- **배포:** GitHub Pages

## 핵심 정의

| 항목 | 내용 |
|---|---|
| 서비스명 | 짬짬이 그리기 |
| 핵심 사용자 | 전자칠판을 사용하는 초등학교 교사 |
| 핵심 상황 | 자투리 5~10분, 비 오는 날 실내 활동, 미술 도입 |
| 핵심 가치 | 화면이 한 획씩 보여 주고, 학생은 종이에 따라 그린다. 교사는 시작만 누른다. |
| 핵심 흐름 | 그림 고르기 → 준비 → 단계별 따라 그리기 → 완성·색칠 → 마무리 |
| MVP 범위 | 그림 18개(3난이도 × 6), 자동·수동 진행, 획 강조, 교사 멘트, 오프라인 |
| 준비물 | 종이와 연필. 색칠 도구는 있으면 씁니다. 인쇄물·학습지 없음 |
| 가족 안에서 | 짬짬이 게임·퀴즈·영상·이야기·쉼·낱말에 이어 **일곱 번째**. 유일하게 손과 종이를 쓴다 |

## 1. 제품 개요

### 1.1 한 문장 정의
교사가 그림 하나를 고르면, 화면이 완성까지의 선을 한 획씩 차례로 보여 주어
학생들이 각자 종이에 따라 그릴 수 있게 하는 전자칠판용 웹서비스입니다.

### 1.2 해결하려는 문제
- 자투리 시간에 쓸 수 있는 **손으로 하는 활동**이 없습니다. 이미 있는 다섯(게임·퀴즈·영상·
  이야기·쉼)과 작업 중인 낱말은 모두 화면을 터치하거나 말하는 활동이고, 종이와 연필을
  쓰는 활동은 비어 있습니다.
- 따라 그리기(directed drawing)를 하려면 교사가 칠판에 직접 그려 가며 진행해야 합니다.
  이때 교사는 칠판 앞에 붙어 있어야 하고, 뒤처진 학생을 돌아볼 수 없습니다.
- 시중 자료는 완성본 그림이나 순서도 이미지여서, **한 획씩 멈춰 가며 보여 주는** 진행이
  안 됩니다. 학생은 어느 선이 새로 생긴 선인지 찾지 못합니다.
- 그림 실력에 자신이 없는 교사는 시작 자체를 꺼립니다.

### 1.3 목표

**목표 1 (최우선) — 교사가 활동을 가장 쉽게 할 수 있게 한다.**
- 홈에서 **3번 이하 클릭**으로 첫 획이 나온다.
- 시작을 누른 뒤에는 **교사 조작 0회로 끝까지** 진행된다(자동 모드가 기본).
- 교사가 교실 뒤에 있어도 **프레젠터 리모컨·키보드**로 조작된다.
- 반이 못 따라오면 **활동 중에 즉시 속도를 낮춘다.**
- 각 단계에 **교사가 그대로 읽어 주면 되는 한 문장**이 화면에 있다.
- 준비물은 종이와 연필뿐이고(색칠은 선택), 인쇄·계정·설치가 없다.

**목표 2 — 초등학생이 쉽게 그릴 수 있는 그림을 다양하게 구성한다.**
- 한 단계는 **한 획 또는 대칭 한 쌍**이다.
- 첫 획이 **크기와 위치를 정한다**(완성본 높이의 40% 이상인 큰 도형).
- **보조선을 쓰지 않는다** — 학생이 지워야 하는 선을 만들지 않는다.
- 매 단계의 누적 형태가 그 자체로 그럴듯하다.
- **비뚤어도 완성되면 그럴듯하다** — 정확한 비율을 요구하는 소재는 넣지 않는다.
- 주제 7군(동물·사람과 표정·식물·음식·사물과 탈것·계절과 행사·상상)에 걸쳐 고른다.

### 1.4 비목표
- 화면에 직접 그리게 하기 — 전자칠판 한 대에 학생 30명이므로 종이가 맞습니다.
- 잘 그렸는지 채점·판정하기.
- 작품 사진 업로드·공유·갤러리 — 아동의 얼굴과 작품은 올리지 않습니다.
- 회원가입·학급 계정·학생 이름 입력.
- 실시간 AI 그림 생성 — 오프라인 동작 원칙과 어긋납니다.
- 미술 교과 대체, 소묘·채색 기법 지도.

## 2. 사용자와 환경

### 주요 사용자
- 1차: 초등학교 교사(그림에 자신이 없는 교사를 포함)
- 2차: 초등학생 1~6학년
- 보조: 돌봄·방과후 강사

### 사용 환경
- 전자칠판, 대형 TV, 프로젝터. 교실 스피커 있음
- Chrome·Edge 등 최신 브라우저
- 학생 개인 기기·계정 없이 전체 참여
- 학교 인터넷이 끊겨도 동작해야 함(PWA)
- 학생 좌석은 화면에서 최대 8~9m 떨어져 있음

## 3. 교사 편의성 설계 (목표 1의 전개)

### 3.1 고르기 — 3번 이하 클릭
- 홈은 **완성 그림 썸네일 카드 그리드** 하나입니다. 글을 읽지 않고 그림으로 고릅니다.
- 카드에 **난이도 · 단계 수 · 예상 시간**을 적습니다(예: `쉬움 · 8단계 · 약 4분`).
  자투리 시간의 길이에 맞춰 고를 수 있어야 합니다.
- 필터는 **난이도 3개 · 주제 7개** 칩뿐입니다. 검색은 보조입니다.
- 카드를 누르면 준비 화면, 준비 화면에서 [시작]. **홈 → 카드 → 시작 = 3클릭.**

### 3.2 진행 — 시작 후 조작 0회
- **자동 모드가 기본값**입니다. 단계마다 데이터에 적힌 권장 시간이 지나면 다음 획으로
  넘어갑니다. 교사는 시작만 누르고 학생들 사이를 돌아다닐 수 있습니다.
  (짬짬이 쉼과 같은 원칙 — 시작 버튼 이후 교사는 아무것도 누르지 않는다.)
- **속도 3단**을 활동 중에 바꿉니다: 느리게(×1.5) · 보통(×1.0) · 빠르게(×0.7).
  반이 못 따라오는 것은 시작 전에 알 수 없으므로, 진행 중 변경이 반드시 가능해야 합니다.
  **지금 단계의 남은 시간에도 바로 적용합니다** — `남은시간 × 새 배율 ÷ 옛 배율`.
  교사가 빠르다고 느끼는 순간은 이미 그 단계 중간이므로, 다음 단계까지 기다리면 늦습니다.
- **수동 모드**는 화면 어디를 눌러도 다음 획입니다. 작은 버튼을 조준하게 하지 않습니다.
  조작 막대 영역만 제외합니다(5.1) — 속도 버튼을 누르려다 다음 획으로 넘어가지 않게 합니다.
- **자동 모드에서 캔버스를 누르면 아무 일도 없습니다.** 전자칠판은 손이나 옷이 스치기 쉬운데,
  실수로 닿은 화면이 진행을 건드리면 "시작 후 조작 0회"가 깨집니다. 자동 모드에서 앞으로
  넘기려면 `Space`·`→` 또는 조작 막대의 [다음]입니다.
- **멈춤**은 한 번에 됩니다. 학생이 뒤처지면 교사가 즉시 세웁니다.
- 자동 진행 타이머는 `setTimeout`을 누적하지 않고 **실제 시각 차이**로 셉니다. 전자칠판
  브라우저가 탭을 백그라운드로 내리면 타이머 간격이 늘어지는데, 시각으로 계산하면 밀리지
  않습니다. 한 번에 **한 단계만** 넘어가게 해서, 오래 가려져 있었어도 여러 단계를 건너뛰지
  않습니다. `requestAnimationFrame`은 쓰지 않습니다 — 창이 가려지면 아예 멈춰서 진행이
  서 버립니다.

### 3.3 원격 조작 — 교실 뒤에서
프레젠터 리모컨은 대개 `PageUp`·`PageDown`을 보냅니다. 그래서 다음 키를 모두 받습니다.

| 키 | 동작 |
|---|---|
| `Space` · `→` · `PageDown` · `Enter` | 다음 획 |
| `←` · `PageUp` | 한 획 뒤로(화면만) |
| `R` | 방금 획 다시 보여 주기 |
| `P` | 자동 진행 멈춤·재개 |
| `1` `2` `3` | 느리게 · 보통 · 빠르게 |
| `C` | 완성본 잠깐 보기 |
| `F` | 전체화면 |
| `Esc` | 나가기(확인 후) |

> **뒤로 가기의 뜻을 분명히 합니다.** 학생이 이미 종이에 그린 선은 되돌릴 수 없습니다.
> 그래서 뒤로 가기의 목적은 취소가 아니라 **놓친 학생에게 다시 보여 주기**입니다.
> 교실에서 더 자주 필요한 것은 한 획 뒤로가 아니라 **방금 획 다시 재생(`R`)**이므로,
> 이쪽을 화면의 기본 버튼으로 둡니다.

- `←`·`PageUp`으로 뒤로 가면 **자동 진행이 함께 멈춥니다**(`P`와 같은 상태). 타이머가 계속
  흐르면 곧 원래 단계로 되돌아가 "다시 보여 주기"가 되지 않습니다. `P` 또는 `→`로 재개합니다.
- `R`은 애니메이션을 다시 재생하고 **그 단계의 타이머도 처음부터 다시 셉니다.** 놓친 학생에게
  시간을 주는 것이 목적이므로, 다시 보여 주면서 시간을 주지 않으면 뜻이 없습니다.
- `C`는 **누르고 있는 동안만** 완성본을 보여 주고 손을 떼면 사라집니다. 토글로 두면 켜 둔 채
  잊게 되고, 학생이 따라 그리기를 멈추고 완성본을 베낍니다.
- **단계 번호로 바로 뛰는 조작은 두지 않습니다.** 숫자 키 `1` `2` `3`이 속도에 배정되어 있어
  단계 번호 입력과 충돌합니다. 앞뒤 이동으로 충분합니다.

### 3.4 뒤쪽 좌석에서도 보이게
- **이번 획**은 굵은 선 + 왼쪽에서 오른쪽으로 그려지는 애니메이션 + 시작점 점.
- **지난 획**은 흐린 선.
- 색만으로 구분하지 않습니다 — **굵기·색·애니메이션 세 가지를 동시에** 씁니다.
  색약 학생과 색이 뭉개지는 프로젝터를 함께 고려한 것입니다.
- 값을 고정합니다. 7.3으로 좌표계가 두 가지뿐이므로 **전 그림에 같은 값**을 씁니다
  (굵기는 `viewBox` 단위).

  | | 굵기 | 색 | 흰 배경 대비 |
  |---|---|---|---|
  | 이번 획 | `7` | 진한 앰버 `#B85C00` | 4.6 : 1 |
  | 지난 획 | `4` | 네이비 `#1D3557` 불투명도 60% | 3.7 : 1 |
  | 시작점 점 | 반지름 `6` | 이번 획과 같은 색 | — |

  밝은 앰버 `#FFB703`(게임의 바탕색)은 흰 배경 대비가 **1.75 : 1**뿐이어서 프로젝터에서
  사라집니다. 그래서 같은 계열의 어두운 값을 씁니다. 두 선 모두 3 : 1을 넘고 이번 획은
  4.5 : 1을 넘습니다.
- 획이 그려지는 애니메이션이 **획의 시작점과 방향**을 저절로 알려 줍니다.
  화살표 기호를 따로 그리지 않아도 됩니다.
- 단계 표시(`3 / 8`)와 남은 시간을 항상 같은 자리에 둡니다.
- **단계 전환 신호음**을 냅니다(교실 스피커 전제, 기본 켜짐, 끌 수 있음).
  종이를 보고 있는 학생이 화면을 안 보고도 다음 획이 나온 것을 압니다.
  오디오 파일을 두지 않고 **WebAudio로 합성**합니다(880Hz → 1175Hz, 각 80ms).
  오프라인 캐시에 자산이 하나 줄고, 파일이 빠져 소리가 안 나는 경우가 없어집니다.

### 3.5 교사 멘트
각 단계에 **읽어 주면 되는 한 문장**을 넣습니다. 교사가 그림 용어를 몰라도 진행됩니다.
18개 사이에서 말이 흔들리지 않도록 **어휘 기준을 4.5에 두었습니다.**

- 화면에 크게 나오는 짧은 말: `큰 동그라미`
- 그 아래 교사 멘트: `종이 가운데에 주먹만큼 큰 동그라미를 그려 보세요.`

준비 화면에는 **종이 방향과 크기 안내**를 둡니다:
`종이를 세로로 놓고, 가운데에 주먹만큼 크게 그릴 거예요.`
— 첫 획의 크기가 그림 전체를 좌우하므로, 시작 전에 말해 주는 것이 가장 효과가 큽니다.

### 3.6 사고 방지
- 활동 중 나가기는 **확인**을 받습니다. 도전 난이도는 색칠까지 8~9분이므로(4.1),
  거의 끝난 활동이 오조작으로 사라지지 않게 합니다.
  확인은 **화면 안에서** 받고 `Enter`(나가기)·`Esc`(계속하기)로 답합니다. 브라우저 기본
  확인창은 8m에서 읽히지 않고, 교실 뒤에 있는 교사가 리모컨으로 답할 수 없습니다.
  확인 중에는 자동 진행이 서고 다른 키를 받지 않습니다.
- 전자칠판이 새로고침되면 **그 단계에서 이어서** 진행합니다.
- **오프라인에서 전 기능이 동작**합니다. 학교 인터넷을 신뢰하지 않습니다.
  다만 PWA는 **한 번은 온라인에서 열어야** 캐시가 생깁니다. 그래서 홈에
  `한 번 열어 두면 인터넷 없이도 됩니다` 한 줄과 캐시 완료 표시를 둡니다.
  14장의 "인터넷을 끊고 확인"은 1회 방문을 마친 뒤의 시나리오입니다.

### 3.7 마무리
- 완성 화면에 완성본을 크게 띄우고 **색칠 타이머**(기본 2분, 조절·건너뛰기 가능)를 둡니다.
  색칠에는 색연필이나 크레파스가 필요하므로 **준비 화면에서 끌 수 있고, 그 선택을
  저장합니다**(7장). 교실의 색칠 도구 사정은 매번 바뀌지 않으므로 한 번 정하면 됩니다.
- [다음 그림]으로 홈을 거치지 않고 바로 다음 활동에 들어갑니다.
- 작품은 **교사가 알아서** 합니다 — 업로드 기능을 두지 않습니다.

## 4. 그림 구성 기준 (목표 2의 전개)

### 4.1 단계 설계
| 난이도 | 대상 | 단계 수 | 획의 성격 | 예상 시간(색칠 제외) |
|---|---|---|---|---|
| 쉬움 | 1~2학년 | 6~8 | 원·타원·직선 위주, 곡선 최소 | 2~3분 |
| 보통 | 3~4학년 | 9~12 | 곡선 포함, 부분 겹침 | 3.5~4.5분 |
| 도전 | 5~6학년 | 13~16 | 곡선·비율·겹침, 세부 표현 | 6~7분 |

7.2의 산출식에 난이도별 획 길이를 넣어 나온 값입니다(단계당 평균 20 · 22 · 27초).
색칠 2분을 더하면 **도전은 8~9분**이므로 자투리 5분에는 맞지 않습니다. 자투리가 짧은
날에는 쉬움·보통이고, 카드의 예상 시간(3.1)이 고르는 자리에서 이것을 알려 줍니다.

규칙:
1. **한 단계 = 학생이 한 번에 이해하는 한 덩어리.** 획 하나, **대칭 한 쌍**(눈 둘·귀 둘 —
   따로 떼면 어색합니다), 또는 **같은 성격의 잔 획 묶음**(수염 세 개씩, 이마 무늬 세 개)입니다.
   한 단계의 획은 **6개까지**입니다. 7장의 `d` 배열이 이것을 담습니다.
2. **첫 획이 크기와 위치를 정합니다.** 첫 단계의 bounding box 높이가 **완성본 bounding box
   높이의 40% 이상**인 큰 도형에서 시작합니다 — 면적이 아니라 **높이** 기준입니다.
   작게 시작하면 종이 한구석에 몰려 완성되지 않습니다.
3. **보조선을 쓰지 않습니다.** 나중에 지워야 하는 선을 만들지 않습니다(지우개 없이 완주).
4. **누적 형태가 매 단계 그럴듯해야 합니다.** 중간에도 무엇을 그리는지 알면 학생이 안심합니다.
5. **획은 한 번에 그을 수 있는 길이**여야 합니다. 한 획이 너무 길면 손이 흔들립니다.

### 4.2 소재 선정 기준
- **비뚤어도 그럴듯한 것만** 넣습니다. 조금 어긋나면 즉시 어색해지는 소재는 넣지 않습니다.
- 얼굴·정면·단순한 실루엣이 유리하고, 측면 전신·기계 구조는 불리합니다.
- 한국 교실 맥락(김밥·눈사람·한복·우산)을 섞어 남의 자료 같지 않게 합니다.

**제외한 소재와 이유**

| 소재 | 이유 |
|---|---|
| 자전거 | 바퀴 두 개와 프레임 비율이 어긋나면 즉시 어색해집니다 |
| 손·다섯 손가락 | 손가락 길이 비율에서 거의 모두 실패합니다 |
| 달리는 네발짐승 측면 | 다리 네 개의 위치·길이를 초등학생이 맞추기 어렵습니다 |
| 국기·태극 문양 | 정확도를 요구하고, 틀린 그림을 교실 화면에 띄우는 위험이 있습니다 |
| 실존 캐릭터 | 저작권 |
| 특정 인물의 얼굴 | 초상, 그리고 닮지 않으면 실패로 느낍니다 |

### 4.3 안전 기준
무서운 형상, 폭력·무기, 종교·정치 상징, 외모를 웃음거리로 삼는 소재는 넣지 않습니다.

### 4.4 MVP 그림 18개

4.2의 선정 기준과 제외 기준을 적용해 확정한 목록입니다. 한국 교실 맥락(김밥·눈사람·
한복·우산)을 섞었습니다.

**쉬움 (1~2학년, 6~8단계)**
1. 고양이 얼굴 — 동물
2. 물고기 — 동물
3. 꽃 한 송이 — 식물
4. 눈사람 — 계절과 행사
5. 자동차 — 사물과 탈것
6. 웃는 얼굴 — 사람과 표정

**보통 (3~4학년, 9~12단계)**
7. 부엉이 — 동물
8. 우주인 — 상상
9. 케이크 — 음식
10. 우산과 장화 — 계절과 행사
11. 로켓 — 사물과 탈것
12. 화분 선인장 — 식물

**도전 (5~6학년, 13~16단계)**
13. 코끼리 — 동물
14. 공룡 — 동물
15. 김밥과 도시락 — 음식
16. 로봇 — 사물과 탈것
17. 한복 입은 아이 — 사람과 표정
18. 용 — 상상

**주제 분포**

| 동물 | 사물과 탈것 | 식물 | 음식 | 계절과 행사 | 사람과 표정 | 상상 |
|---|---|---|---|---|---|---|
| 5 | 3 | 2 | 2 | 2 | 2 | 2 |

7군이 모두 2개 이상이고 최대가 5개입니다. 난이도마다 6개씩이고, 어느 난이도에서도 한 주제가
절반을 넘지 않습니다.

**조정한 것과 이유**

| 바꾼 것 | 이유 |
|---|---|
| `공룡`의 주제를 상상 → **동물** | 공룡은 실존한 동물입니다. 상상 군을 채우려고 붙인 태그였습니다 |
| `나비` → **우주인**(상상) | 나비는 좌우 날개가 그림의 대부분을 차지해, 한쪽이 커지면 그림 전체가 기울어 보입니다. 화면은 대칭 한 쌍을 동시에 보여 주지만, 종이에서 두 번째 날개를 첫 날개에 맞추는 일은 학생 몫입니다. 우주인은 헬멧이 정면 원이라 비뚤어도 그럴듯합니다 |
| `고래와 파도` → **용**(상상) | 고래를 두면 도전 6개 중 동물이 3개가 되고, 쉬움의 `물고기`와 물속 생물 측면 실루엣이 겹칩니다. 용은 곡선·겹침·세부가 있어 도전 난이도에 맞습니다 |

용은 4.3의 안전 기준을 지켜 **둥근 얼굴에 불과 이빨 없이** 그립니다.

### 4.5 멘트 어휘 기준

교사가 **화면을 그대로 읽으면 되는** 것이 3.5의 전제입니다. 읽다가 한 번이라도 "이게 무슨
뜻이지" 하고 멈추면 전제가 깨지므로, 18개 사이에서 말을 통일합니다.

**형식**

| | 규칙 |
|---|---|
| `label`(화면의 짧은 말) | 명사구, **1~8자**, 마침표 없음. `큰 동그라미` · `귀 두 개` · `코` |
| `teacherSay`(교사 멘트) | **한 문장**, 15~40자, `~요.`·`~세요.`로 끝냅니다 |
| `setupSay`(준비 화면 안내) | **한 문장**, 20~50자. **종이 방향과 첫 획 크기**를 반드시 담습니다 |

두 문장으로 나누지 않습니다. 교사가 두 번째 문장을 읽는 동안 학생은 이미 첫 문장을
그리고 있어서, 뒤 문장이 들리지 않습니다.

**말하는 방식**

1. **크기는 몸으로 말합니다.** `주먹만큼` · `손가락 한 마디만큼` · `엄지손톱만큼`.
   수치를 쓰면 학생이 자를 꺼냅니다.
2. **위치는 이미 그린 것을 기준으로 말합니다.** `동그라미 위에` · `코 아래에`.
   종이를 기준으로 말하면(`왼쪽 위에`) 첫 획 위치가 학생마다 달라 그 뒤가 전부 어긋납니다.
3. **첫 단계 멘트에는 종이의 어디인지를 반드시 넣습니다.** `종이 가운데에` · `종이 아래쪽에`.
   첫 획이 크기와 위치를 정하므로(4.1의 2번), 이 한 마디의 효과가 가장 큽니다.
4. **개수는 한글로 셉니다.** `두 개` · `세 개씩`. 읽어 줄 때 자연스럽습니다.
5. **동작을 끝까지 말합니다.** `동그란 눈 두 개.`처럼 명사로 끊지 않고 `~그려요.`로 닫습니다.
   명사로 끊으면 교사가 뒤에 말을 스스로 붙이게 되고, 그 순간 화면을 읽는 것이 아닙니다.

**쓰지 않는 말**

| 갈래 | 쓰지 않음 | 대신 |
|---|---|---|
| 수치 | `cm` `mm` `%` `1/3` `2배` | `주먹만큼` `손가락 한 마디만큼` |
| 교과 도형 용어 | `삼각형` `사각형` `타원` `반원` | `세모` `네모` `길쭉한 동그라미` `반 동그라미` |
| 그림 용어 | `윤곽선` `명암` `비율` `원근` `데생` | 쓰지 않습니다 |
| 숫자·글자 모양 비유 | `3처럼` `U자로` `S자` | 방향과 위치로 말합니다 |
| 잘못을 지적하는 말 | `똑바로` `정확히` `비뚤어지지 않게` `예쁘게` | 쓰지 않습니다 |

**도형 이름은 `동그라미 · 세모 · 네모`로 통일합니다.** 저학년이 아는 말이고, 교과 용어와
섞어 쓰면 같은 모양을 그림마다 다르게 부르게 됩니다.

**숫자·글자 모양 비유를 막는 이유**는 읽어 줄 때 구별이 안 되기 때문입니다. `옆으로 누운
삼처럼`은 숫자 3인지 한자인지 들어서 알 수 없고, 학생은 획을 거꾸로 잡습니다.

**잘못을 지적하는 말을 막는 이유**는 1.4의 비목표(채점하지 않음)와 16장의 원칙(비뚤어도
완성되면 그럴듯하다)입니다. `똑바로`가 한 번 들어가면 그 원칙이 그 단계에서 무너집니다.

### 4.6 단계 예시 — 고양이 얼굴 (쉬움 · 8단계)

4.5의 기준을 적용한 결과입니다.

| 단계 | 화면의 짧은 말 | 교사 멘트 |
|---|---|---|
| 1 | 큰 동그라미 | 종이 가운데에 주먹만큼 큰 동그라미를 그려 보세요. |
| 2 | 귀 두 개 | 동그라미 위에 세모 귀를 좌우로 하나씩 올려요. |
| 3 | 귀 안쪽 | 귀 안에 작은 세모를 하나씩 넣어요. |
| 4 | 눈 두 개 | 얼굴 가운데보다 조금 위에 동그란 눈을 두 개 그려요. |
| 5 | 코 | 눈 사이 아래에 작은 세모 코를 그려요. |
| 6 | 입 | 코 아래에 짧은 줄 두 개를 좌우로 휘어 그려요. |
| 7 | 수염 | 코 양쪽으로 짧은 줄을 세 개씩 뻗어 그려요. |
| 8 | 무늬 | 이마에 짧은 줄 두세 개로 무늬를 넣어요. |

4·5단계는 명사로 끊겨 있었고(4.5의 5번), 6단계는 `옆으로 누운 삼처럼`이 숫자 모양 비유여서
고쳤습니다. 1단계는 종이 위치를 이미 말하고 있어 그대로 둡니다.

## 5. 화면
1. 홈 — 그림 고르기(카드 그리드 · 난이도·주제 필터 · 즐겨찾기 · 최근 사용)
2. 준비 — 완성본 · 종이 방향과 크기 안내 · 속도 선택 · [시작]
3. 따라 그리기 — 캔버스 · 이번 획 강조 · 짧은 말과 교사 멘트 · 단계 표시 · 조작 막대
4. 완성 — 완성본 크게 · 색칠 타이머
5. 마무리 — [다음 그림] · [홈으로]

### 5.1 따라 그리기 배치 — 세로 그림을 가로 화면에

그림은 대부분 세로(`0 0 400 500`)이고 전자칠판은 가로 16:9입니다. **캔버스 왼쪽 · 글자
오른쪽 2단**으로 놓습니다.

- 캔버스는 **화면 높이의 92%**를 채우고, 세로 그림에서 폭은 화면 폭의 약 41%가 됩니다
  (1920×1080에서 약 795×994, 1280×720에서 약 530×662 — 두 해상도에서 비율이 같습니다).
- 남는 오른쪽 단이 글자 자리입니다. 캔버스를 가운데 두고 글자를 아래에 깔면 캔버스가
  글자와 높이를 나눠 쓰게 되어 작아지고, 8m 가독성이 무너집니다. 그래서 2단입니다.
- 가로 그림(`0 0 500 400`)은 캔버스가 화면 폭의 약 65%를 쓰고 오른쪽 단이 좁아집니다.
  같은 2단을 유지하고 글자 크기만 비기능 요구의 최소값(48px·32px)까지 줄입니다.
- 오른쪽 단 배치는 위에서 아래로 **단계 표시와 남은 시간 → 짧은 말 → 교사 멘트 →
  조작 막대**입니다. 단계 표시가 항상 같은 자리에 있어야 한다는 3.4를 이 순서가 지킵니다.
- 수동 모드의 "화면 어디나 눌러 다음 획"에서 **조작 막대 영역은 제외**합니다. 속도 버튼을
  누르려다 다음 획으로 넘어가지 않게 하는 것이고, 2단 배치가 이 제외 영역을 화면 한쪽
  아래로 모아 줍니다.

## 6. 기능 요구사항

| ID | 기능 | 우선순위 |
|---|---|---|
| FR-01 | 완성 썸네일 카드 그리드로 그림 고르기 | Must |
| FR-02 | 난이도·주제 필터 | Must |
| FR-03 | 카드에 난이도·단계 수·예상 시간 표시 | Must |
| FR-04 | 준비 화면 — 완성본 + 종이 방향·크기 안내 | Must |
| FR-05 | 단계별 획 공개 + 그려지는 애니메이션 | Must |
| FR-06 | 이번 획 강조 / 지난 획 흐리게 | Must |
| FR-07 | 자동 진행(단계별 권장 시간, 기본 모드) | Must |
| FR-08 | 속도 3단 — 활동 중 즉시 변경 | Must |
| FR-09 | 수동 진행 — 화면 어디나 눌러 다음 획 | Must |
| FR-10 | 키보드·프레젠터 리모컨 조작 | Must |
| FR-11 | 방금 획 다시 보여 주기 | Must |
| FR-12 | 한 획 뒤로(화면만) | Must |
| FR-13 | 자동 진행 멈춤·재개 | Must |
| FR-14 | 단계 표시와 남은 시간 | Must |
| FR-15 | 단계 전환 신호음(기본 켜짐, 끌 수 있음) | Must |
| FR-16 | 단계별 교사 멘트 표시 | Must |
| FR-17 | 완성 화면 + 색칠 타이머(준비 화면에서 끌 수 있음) | Must |
| FR-18 | 전체화면 | Must |
| FR-19 | 활동 중 나가기 확인 | Must |
| FR-20 | 오프라인 실행(PWA) | Must |
| FR-21 | 자매 사이트 바로가기 | Must |
| FR-22 | 새로고침 후 그 단계에서 이어하기 | Must |
| FR-23 | 즐겨찾기 | Should |
| FR-24 | 최근 사용 표시 | Should |
| FR-25 | [다음 그림] 바로 시작 | Should |
| FR-26 | 완성본 잠깐 보기(누르고 있는 동안만) | Must |
| FR-27 | 그림 무작위 추천 | Could |
| FR-28 | 음성으로 교사 멘트 읽어 주기 | Could |

## 7. 데이터 구조

그림 하나가 파일 하나입니다(`drawings/<id>/drawing.json`).

```json
{
  "id": "cat-face",
  "title": "고양이 얼굴",
  "theme": "animal",
  "difficulty": "easy",
  "grades": ["lower"],
  "paper": "portrait",
  "viewBox": "0 0 400 500",
  "setupSay": "종이를 세로로 놓고, 가운데에 주먹만큼 크게 그릴 거예요.",
  "coloringSeconds": 120,
  "steps": [
    {
      "d": ["M 120 200 A 80 80 0 1 1 280 200 A 80 80 0 1 1 120 200"],
      "label": "큰 동그라미",
      "teacherSay": "종이 가운데에 주먹만큼 큰 동그라미를 그려 보세요."
    },
    {
      "d": [
        "M 140 145 L 155 95 L 190 125",
        "M 260 145 L 245 95 L 210 125"
      ],
      "label": "귀 두 개",
      "teacherSay": "동그라미 위에 세모 귀를 좌우로 하나씩 올려요."
    }
  ],
  "keywords": ["동물", "고양이", "얼굴"]
}
```

### 7.1 `d`는 항상 배열입니다

원소 하나가 획 하나이고, **한 단계의 원소는 같은 시각에 함께 그려집니다.** 획 하나뿐인
단계도 원소 하나인 배열로 씁니다 — 형식을 갈래로 나누지 않습니다.

4.1의 1번(대칭 한 쌍·잔 획 묶음이 한 단계)을 이 배열이 담습니다. 한 문자열 안에 `M`을 두 번
넣어 subpath로 만들지 **않습니다.** `stroke-dasharray` 애니메이션이 subpath를 만나면 좌우를
순차로 그리고, subpath 사이의 이동 구간까지 `getTotalLength()`에 들어가 획이 중간에 멈춘
것처럼 보입니다. 귀 둘·눈 둘이 동시에 나타나야 하므로 원소를 나눕니다.

**명령은 `M` · `L` · `C` · `Z` 만 씁니다. 호(`A`)를 쓰지 않습니다.** 9장의 bounding box 검증이
제어점 기준인데, 호는 제어점이 시작점과 끝점뿐이어서 bounding box가 뜻을 잃습니다. 반지름
110인 원을 호 두 개로 적으면 제어점 높이가 0이 되어 첫 획 40% 검증이 **잘못 실패**하고,
viewBox 밖으로 부풀어도 **잘못 통과**합니다. 원과 곡선은 3차 베지어(`C`)로 적습니다
(원은 네 구간, 제어점 거리 `0.5523 × 반지름`). 상대 좌표 소문자 명령도 쓰지 않습니다.

### 7.2 `seconds`는 획 길이에서 계산합니다

자동 모드가 기본값이므로 `seconds`가 곧 제품 품질입니다. 18개를 사람의 감으로 적으면
그림 사이 기준이 흔들리므로, 기본값을 길이에서 냅니다.

```text
길이비율 = (d 원소들의 getTotalLength() 합) ÷ (viewBox 대각선 길이)
seconds  = round5( min( 15 + 12 × 길이비율, 40 ) )
```

- `15`초는 화면에서 종이로 눈을 옮기고 연필을 대는 **고정 시간**입니다. 교실에서는 이
  고정 시간이 획을 긋는 시간보다 큽니다.
- `12`초는 획을 따라 그리는 **길이당 시간**입니다.
- `round5`는 5초 단위 반올림이고, 상한은 40초입니다. 결과 범위는 15~40초입니다.
- 대칭 한 쌍은 길이를 **합산**합니다. 학생이 둘 다 그려야 하므로 시간도 두 배 듭니다.
- 위 예시로 확인하면 큰 동그라미는 25초, 귀 두 개는 20초가 나옵니다.

`steps[].seconds`는 **생략이 기본**이고, 계산값이 실제와 어긋나는 단계에만 적어 덮어씁니다.
교실 테스트 뒤에는 **계수 두 개(`15`·`12`)만 고쳐 18개 전체를 함께 옮깁니다.** 그림마다
숫자를 다시 적는 일이 생기지 않게 하는 것이 이 규칙의 목적입니다.

### 7.3 `viewBox`는 두 가지만 씁니다

`0 0 400 500`(세로) 또는 `0 0 500 400`(가로)뿐이고, `paper`가 둘 중 하나를 가리킵니다.
그림마다 좌표계가 다르면 5.1의 캔버스 비율과 7.2의 길이비율이 그림마다 달라지고,
선 굵기 값도 그림마다 달라집니다. 두 가지로 묶으면 이 셋이 모두 고정됩니다.

**완성본을 따로 두지 않습니다.** 완성본은 전 단계 `d`를 겹쳐 그린 것이고, 썸네일도
같은 방식으로 만듭니다. 완성본 이미지를 별도 자산으로 두면 단계를 고칠 때 어긋나는데,
합성으로 만들면 **어긋날 수 없습니다**. (짬짬이 게임에서 탭 아이콘과 헤더 로고를
한 파일로 합친 것과 같은 이유입니다.)

`단계 수`·`예상 시간`은 저장하지 않고 `steps`에서 계산합니다 — 단일 소스 원칙.

로컬에 저장하는 것은 **즐겨찾기·최근 사용·속도·소리 설정·색칠 타이머 사용 여부·진행 중이던
단계**뿐입니다.
학생 이름과 작품은 저장하지 않습니다.

## 8. 진행 상태
```text
HOME → READY → DRAW(1..n) → DONE → COLOR → COMPLETE
```
- `DRAW`에서 자동 진행은 `seconds × 속도배율` 뒤 다음 단계로 갑니다. 시간은 단계 시작
  시각과 현재 시각의 차이로 셉니다(3.2).
- `P`로 멈추면 타이머만 서고 화면은 그대로입니다. `←`로 뒤로 갈 때도 함께 멈춥니다(3.3).
- 속도를 바꾸면 지금 단계의 남은 시간이 `남은시간 × 새 배율 ÷ 옛 배율`로 다시 잡힙니다.
- `R`은 그 단계의 타이머를 처음부터 다시 셉니다.
- `COLOR`는 건너뛸 수 있고, 준비 화면에서 껐으면 `DONE` 다음에 바로 `COMPLETE`입니다.

## 9. 검증

정적 검증(`npm test`)에서 그림 데이터를 전수 확인합니다.

- `steps` 개수가 난이도별 범위 안인지(쉬움 6~8 · 보통 9~12 · 도전 13~16)
- `d`가 배열이고 원소가 **6개 이하**인지(4.1의 1번)
- 각 원소가 유효한 SVG path이고 **`M`이 하나뿐**이며(7.1의 subpath 금지),
  **`M`·`L`·`C`·`Z` 만 쓰고 호(`A`)와 소문자 명령이 없는지**(7.1)
- 모든 제어점이 `viewBox` 안인지 — 곡선의 실제 bounding box는 제어점 bounding box보다
  작으므로, 제어점 기준 검사는 보수적이고 통과하면 안전합니다
- **첫 단계의 bounding box 높이가 완성본 bounding box 높이의 40% 이상인지** — 면적이 아니라
  높이 기준입니다(4.1의 2번). bounding box는 제어점 기준으로 계산합니다
- `label`이 1~8자이고 마침표가 없는지(4.5)
- `teacherSay`가 **한 문장**(끝의 `.` 하나뿐, 중간에 `.` `!` `?` 없음)이고 15~40자이며
  `요.`로 끝나는지
- `teacherSay`·`label`에 4.5의 **쓰지 않는 말**이 없는지 — 낱말 목록을 스크립트에 그대로
  둡니다. `타원`·`삼각형`처럼 두 글자 이상이고 다른 낱말에 섞이지 않는 것만 기계로 보고,
  나머지는 15장 3번의 검토 시트에서 사람이 봅니다
- **첫 단계 `teacherSay`에 `종이`가 들어 있는지** — 4.5의 3번을 기계로 지킵니다
- `setupSay`가 20~50자 한 문장이고, `paper` 값에 맞는 방향 말(`세로`·`가로`)이 들어 있는지
- `seconds`를 직접 적은 단계는 15~40초 안인지(7.2), `difficulty`·`theme`·`paper`가 허용 값인지
- `viewBox`가 `0 0 400 500`·`0 0 500 400` 중 하나이고 `paper`와 맞는지(7.3)
- `id` 중복이 없고 목록과 폴더가 일치하는지

브라우저 검증에서는 자동 진행으로 마지막 단계까지 도달하고 콘솔 오류가 0인지 봅니다.

## 10. 비기능 요구사항
- 첫 화면 2초 이내
- 오프라인에서 전 기능 동작
- 전자칠판에서 짧은 말 최소 48px, 교사 멘트 최소 32px 상당
- 선 굵기와 배경 대비는 3.4의 표에 고정(이번 획 4.6 : 1 · 지난 획 3.7 : 1),
  색만으로 정보를 전하지 않음
- 개인정보 입력 없음, 외부 요청 없음
- 키보드만으로 전 기능 조작 가능, 포커스 표시 명확
- JSON 추가만으로 그림 확장
- 최신 Chrome·Edge 우선

## 11. 기술 구성
- HTML · CSS · Vanilla JavaScript, 빌드 단계 없음
- SVG(`stroke-dasharray` 애니메이션으로 획 그리기). 한 단계의 `d` 원소들은 각각 `path`
  하나이고 같은 시각에 함께 그립니다(7.1)
- 그림 데이터 JSON + 목록 통합본 1요청
- LocalStorage, 서비스 워커(PWA). 캐시는 첫 온라인 방문에서 만들어집니다(3.6)
- 신호음은 WebAudio 합성, 오디오 파일 없음(3.4)
- 자동 진행 타이머는 시각 차이 기준, `setTimeout` 누적 금지(3.2)
- GitHub Pages. 서버·DB 없음

## 12. 저장소와 가족 연결

**새 저장소 `jjam-draw`로 만듭니다.** 짬짬이 쉼·낱말과 같은 방식이고, 짬짬이 가족은
일곱이 됩니다.

- 공통 파일(`shared/jjam-switcher.js`, 웹폰트, `browser.yml`)은 **상류인 `jjam`에 먼저**
  반영하고 `npm run sync:shared`로 받습니다. 직접 고치면 동기화 검사가 깨집니다(전례 있음).
- 헤더 바로가기에 `draw`를 추가하는 작업은 **`jjam`에서 시작**해 다른 저장소로 내려갑니다.
- 이 PRD 파일은 새 저장소를 만들 때 그 저장소 루트로 옮깁니다(짬짬이 이야기 선례).

### favicon

가족 공통 틀(둥근 타일 + 우상단 흰 원에 네이비 시계 배지)을 그대로 쓰고 바탕색과 심볼만
다르게 합니다. 심볼은 **연필**, 바탕색은 **코랄 `#E4572E`**.

| 사이트 | 바탕색 | 색상각 |
|---|---|---|
| 게임 | 앰버 `#FFB703` | 43° |
| 퀴즈 | 그린 `#12A57C` | 165° |
| 쉼 | 티일 `#0E7C86` | 185° |
| 영상 | 스카이 `#4FA8E8` | 208° |
| 이야기 | 퍼플 `#6145B5` | 254° |
| **그리기** | **코랄 `#E4572E`** | **14°** |

기존 다섯은 43°~254°에 모여 있고 **254°에서 43°로 넘어가는 구간(자홍·붉은색)이 비어**
있습니다. 코랄을 그 자리에 넣으면 다섯 중 어느 것과도 가장 멀어집니다.

가장 가까운 이웃은 앰버(43°, 29° 차이)인데, 채도와 명도가 뚜렷이 달라 작은 탭 아이콘에서도
"노란 타일"과 "붉은 타일"로 갈립니다. 다만 이것은 색상값 계산이고, **실제로 나란히 렌더링해
확인하는 절차를 개발 순서 9번에 넣었습니다.** #46에서 영상의 네이비가 어두운 테마에서 묻히는
것을 렌더링해 보고서야 발견한 전례가 있어, 값만 정해 두고 넘어가지 않습니다.

### 검토했으나 택하지 않은 대안

`jjam` 안의 게임으로 넣으면 기존 검증·CI를 그대로 타서 더 빠릅니다. 다만 게임 105종
그리드에 묻히고, 자동 진행·리모컨 조작처럼 이 활동에만 필요한 조작 체계를 게임 공통 틀에
넣어야 해서 목표 1과 충돌합니다.

## 13. MVP 필수 범위
- 그림 18개(쉬움·보통·도전 각 6개), 주제 7군에 분산
- 홈·준비·따라 그리기·완성·마무리 5화면
- 자동 진행 + 속도 3단 + 수동 진행
- 키보드·리모컨 조작
- 획 강조와 그려지는 애니메이션
- 단계별 짧은 말과 교사 멘트
- 단계 전환 신호음, 전체화면
- 색칠 타이머
- 오프라인 실행
- 자매 사이트 바로가기

## 14. 완료 기준
- 홈에서 **3클릭 안에** 첫 획이 나온다
- 시작 후 **한 번도 누르지 않고** 마지막 단계까지 도달한다
- 활동 중 속도를 바꾸면 **지금 단계의 남은 시간부터** 반영된다
- 프레젠터 리모컨(`PageUp`·`PageDown`)으로 앞뒤 이동이 된다
- 어느 단계에서도 **지금 그릴 획을 3초 안에 알아볼 수 있다**
- 18개 전부 마지막 단계 누적 형태가 완성본과 같다(합성이므로 구조적으로 보장)
- 쉼표 하나까지 오프라인에서 동작한다 — 한 번 방문해 캐시한 뒤 인터넷을 끊고 전 과정 확인
- 새로고침 후 그 단계에서 이어진다
- 8m 뒤에서 화면의 획과 짧은 말이 읽힌다
- 전자칠판 1280·1920px에서 넘침 없음
- GitHub Pages에서 오류 없이 실행

## 15. 개발 순서
1. 저장소 `jjam-draw` 생성, 이 PRD를 저장소 루트로 옮김
2. **데이터 형식과 정적 검증 스크립트** — 형식을 먼저 고정합니다. 형식이 흔들린 채로
   그림 18개를 만들면 전부 다시 손봐야 합니다
3. **획 저작 도구와 단계 검토 시트** — `tools/` 아래 로컬 HTML 두 장이고 배포에는 넣지
   않습니다. **그림 제작(7번)보다 먼저** 만듭니다
   - **저작 도구**: 참조 그림을 배경에 깔고 획 단위로 그으면 `steps[].d` 배열이 나오는
     화면입니다. 획을 하나 그을 때마다 그 단계까지의 누적 형태를 옆에 보여 주어, 4.1의
     4번(누적 형태가 매 단계 그럴듯함)을 만들면서 확인합니다. 18개 합계 200개에 가까운
     `d` 문자열을 손으로 적는 것은 현실적이지 않고, 이것이 MVP 작업량의 절반입니다.
     멘트를 적는 자리에서 4.5의 길이·어미·쓰지 않는 말을 그 자리에서 표시합니다 — 18개를
     다 만든 뒤 검증에서 걸리면 문장을 200개 고쳐야 합니다
   - **검토 시트**: 18개 전 그림의 전 단계 누적 형태를 한 페이지 격자로 뽑습니다(약 200장).
     4.1의 4번은 기계로 판정할 수 없어 사람이 봐야 하는데, 그림을 하나씩 열어 단계를
     넘겨 가며 보는 방식으로는 실제로 하지 않게 됩니다
4. 그림 1개(고양이 얼굴)로 따라 그리기 화면 완성 — 획 강조·애니메이션까지
5. 자동 진행·속도·멈춤·다시 보기·리모컨 조작 (목표 1의 핵심을 먼저 세움)
6. 홈·준비·완성·마무리 화면
7. 그림 나머지 17개 제작 — 3번의 저작 도구로 만들고 검토 시트로 전수 확인
8. 소리·전체화면·이어하기·즐겨찾기
9. **favicon·홈 화면 아이콘 제작 + 일곱 사이트를 나란히 렌더링해 구분 확인**
   (밝은·어두운 테마 양쪽), 가족 공통 파일 동기화, 자매 바로가기(상류 `jjam`부터)
10. PWA·오프라인 — 인터넷을 끊고 전 과정 확인
11. 전자칠판 실화면 점검(8m 가독성 · 1280·1920px 넘침), 7.2의 계수 두 개 조정
12. GitHub Pages 배포 → 교실 테스트 후 수정

## 16. 제품 원칙

> **교사는 시작만 누르고, 화면이 한 획씩 이끈다. 비뚤어도 완성되면 그럴듯하다.**

모든 기능은 `3클릭 안에 시작`, `시작 후 조작 0회`, `한 단계 한 획`, `지우개 없이 완주`
원칙을 따릅니다.
